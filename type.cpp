#include <cstring>
#include <fstream>

using namespace std;

using u64 = unsigned long long;
using i64 = signed long long;
typedef pair<int, int> coord;

enum dir {
    Right,
    Up,
    Left,
    Down,
};

